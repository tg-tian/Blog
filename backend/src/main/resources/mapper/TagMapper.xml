<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.tg.blog.backend.dao.TagMapper">

    <insert id="insert" parameterType="com.tg.blog.backend.entity.Tag">
        INSERT INTO tag (name, slug, description, color)
        VALUES (#{name}, #{slug}, #{description}, #{color})
    </insert>

    <update id="update" parameterType="com.tg.blog.backend.entity.Tag">
        UPDATE tag SET
            name = #{name},
            slug = #{slug},
            description = #{description},
            color = #{color}
        WHERE id = #{id}
    </update>

    <delete id="deleteById" parameterType="long">
        DELETE FROM tag WHERE id = #{id}
    </delete>

    <select id="selectById" resultType="com.tg.blog.backend.entity.Tag" parameterType="long">
        SELECT id, name, slug, description, color FROM tag WHERE id = #{id}
    </select>

    <select id="selectAll" resultType="com.tg.blog.backend.entity.Tag">
        SELECT id, name, slug, description, color FROM tag
    </select>
    
    <select id="selectByIds" resultType="com.tg.blog.backend.entity.Tag">
        SELECT id, name, slug, description, color FROM tag
        WHERE id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
    </select>
</mapper>
