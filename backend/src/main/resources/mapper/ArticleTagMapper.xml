<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.tg.blog.backend.dao.ArticleTagMapper">

    <insert id="insertArticleTag">
        INSERT INTO article_tag (article_id, tag_id)
        VALUES (#{articleId}, #{tagId})
    </insert>

    <insert id="batchInsertArticleTags">
        INSERT INTO article_tag (article_id, tag_id)
        VALUES
        <foreach collection="tagIds" item="tagId" separator=",">
            (#{articleId}, #{tagId})
        </foreach>
    </insert>
    
    <select id="selectTagsByArticleId" resultType="long">
        SELECT tag_id FROM article_tag WHERE article_id = #{articleId}
    </select>

    <select id="selectArticleTagByArticleIds" resultType="com.tg.blog.backend.entity.ArticleTagRelation">
        SELECT article_id AS articleId, tag_id AS tagId
        FROM article_tag
        WHERE article_id IN
        <foreach collection="articleIds" item="articleId" open="(" separator="," close=")">
            #{articleId}
        </foreach>
    </select>

    <delete id="deleteArticleTag">
        DELETE FROM article_tag WHERE article_id = #{articleId} AND tag_id = #{tagId}
    </delete>

    <delete id="deleteByArticleId">
        DELETE FROM article_tag WHERE article_id = #{articleId}
    </delete>
    

</mapper>
